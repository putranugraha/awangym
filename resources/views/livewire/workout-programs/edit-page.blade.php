<div class="awan-page"><div class="page-heading"><h1>Edit Program</h1><a class="secondary-btn" href="{{route('trainer.programs')}}" wire:navigate>Kembali</a></div>@if(session('success'))<div class="notice">{{session('success')}}</div>@endif
<form wire:submit="save" class="form-card"><label>Nama<input class="form-input" wire:model="program_name"></label><div class="form-grid"><label>Target<input class="form-input" wire:model="target_goal"></label><label>Level<select class="form-input" wire:model="difficulty_level"><option value="beginner">Beginner</option><option value="intermediate">Intermediate</option><option value="advanced">Advanced</option></select></label></div><div class="form-grid"><label>Durasi<input class="form-input" type="number" wire:model="duration_weeks"></label><label>Status<select class="form-input" wire:model="program_status"><option value="active">Aktif</option><option value="inactive">Nonaktif</option></select></label></div><label>Deskripsi<textarea class="form-input" wire:model="description"></textarea></label><button class="primary-btn">Simpan Perubahan</button></form>
<section class="form-card"><h2>Susunan Latihan</h2><div class="exercise-list">@foreach($items as $item)<div wire:key="item-{{$item->program_exercise_id}}"><strong>Hari {{$item->training_day}} · {{$item->exercise->exercise_name}}</strong><span>{{$item->sets}} set × {{$item->repetitions}}</span></div>@endforeach</div><form wire:submit="addExercise" class="inline-form"><select class="form-input" wire:model="exercise_id"><option value="">Pilih latihan</option>@foreach($exercises as $e)<option value="{{$e->exercise_id}}">{{$e->exercise_name}}</option>@endforeach</select><input class="form-input" type="number" wire:model="training_day" placeholder="Hari"><input class="form-input" type="number" wire:model="sets" placeholder="Set"><input class="form-input" wire:model="repetitions" placeholder="Repetisi"><button class="secondary-btn">+ Latihan</button></form></section>
<section class="form-card"><h2>Tetapkan ke Member</h2><form wire:submit="assign" class="inline-form"><select class="form-input" wire:model="member_id"><option value="">Pilih member</option>@foreach($members as $m)<option value="{{$m->member_id}}">{{$m->user->full_name}}</option>@endforeach</select><input class="form-input" type="date" wire:model="start_date"><input class="form-input" wire:model="trainer_notes" placeholder="Catatan"><button class="primary-btn">Tetapkan</button></form></section></div>
