<div class="awan-page"><div class="page-heading"><h1>Program Latihan</h1><a class="primary-btn" href="{{route('trainer.programs.create')}}" wire:navigate>+ Buat Program</a></div>@if(session('success'))<div class="notice">{{session('success')}}</div>@endif @forelse($programs as $p)<article class="list-card" wire:key="program-{{$p->program_id}}"><div class="grow"><span class="chip">{{ucfirst($p->difficulty_level)}}</span><h3>{{$p->program_name}}</h3><p>{{$p->target_goal}} · {{$p->duration_weeks}} minggu · {{$p->exercises->count()}} latihan</p></div><a class="edit-link" href="{{route('trainer.programs.edit',$p)}}" wire:navigate>Edit</a></article>@empty<div class="empty-card">Belum ada program</div>@endforelse</div>
