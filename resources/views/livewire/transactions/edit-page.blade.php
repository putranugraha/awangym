<div class="awan-page"><div class="page-heading"><h1>Edit Transaksi</h1><a class="secondary-btn" href="{{route('admin.transactions')}}" wire:navigate>Kembali</a></div><article class="summary-card"><h2>{{$transaction->member->user->full_name}}</h2><p>{{$transaction->invoice_number}} · Rp {{number_format($transaction->amount,0,',','.')}}</p></article><form wire:submit="save" class="form-card"><div class="form-grid"><label>Metode<select class="form-input" wire:model="payment_method"><option value="cash">Tunai</option><option value="transfer">Transfer</option><option value="e_wallet">E-wallet</option></select></label><label>Status<select class="form-input" wire:model="payment_status">@foreach(['pending','paid','failed','refunded'] as $s)<option value="{{$s}}">{{ucfirst($s)}}</option>@endforeach</select></label></div><label>Catatan<textarea class="form-input" wire:model="notes"></textarea></label>@if($errors->any())<div class="error-box">{{$errors->first()}}</div>@endif<button class="primary-btn">Simpan Perubahan</button></form></div>
