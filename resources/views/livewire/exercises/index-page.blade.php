<div class="awan-page"><div class="page-heading"><h1>Daftar Latihan</h1><a class="primary-btn" href="{{route('trainer.exercises.create')}}" wire:navigate>+ Tambah</a></div>@if(session('success'))<div class="notice">{{session('success')}}</div>@endif @forelse($exercises as $e)<article class="list-card" wire:key="exercise-{{$e->exercise_id}}"><div class="grow"><span class="chip">{{$e->category}}</span><h3>{{$e->exercise_name}}</h3><p>{{$e->description}}</p></div><a class="edit-link" href="{{route('trainer.exercises.edit',$e)}}" wire:navigate>Edit</a></article>@empty<div class="empty-card">Belum ada latihan</div>@endforelse</div>
